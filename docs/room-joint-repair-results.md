# 联合修补候选迭代（2026-09-19）

## 已实施

- 断线端点可以投影连接到现有墙段内部，不再只考虑端点对端点；候选仍需 Jev 判断。
- `room-repair-plans.ts` 枚举单连接和双连接组合，使用实际空间校验生成完整候选。只记录该轮廓实际使用的虚拟连接，不把无关连接一并批准。
- 组合候选由 Jev 审查完整轮廓和未批准连接；最终接受才能批准该组合。拒绝所有候选后，即使存在几何闭合轮廓，下一轮仍尝试联合修补。
- 增加双断口、搜索截断、端点投影、拒绝后继续搜索、联合修补批准权限的回归覆盖。

## 实测结果

统一配置报告：`reports/room-v2/joint-final-evaluation/summary.json`。六个房间仍为 **0/6 接受**，不能声称识别成功率改善。

| 房间 | 结果 | 最终候选 |
|---|---|---|
| 资料室 | incomplete | 无 |
| 冷链室 | incomplete | 无 |
| 接种室 | incomplete | 无 |
| 哺乳室 | incomplete | 3.477㎡，存在障碍物冲突 |
| 留观区 | Jev 拒绝 | 455.683㎡ |
| 登记室 | Jev 拒绝 | 0.135㎡ |

最终运行共引用 141 条成功决策记录，新增模型请求为 0：复用了 `joint-projection-evaluation` 中请求证据完全一致的成功响应。代码和几何流程重新执行，但不是独立模型重复评测。六份报告的代码、输入和预算哈希一致。登记室严格离线回放与原报告一致（仅排除回放/复用来源及新增请求计数）。

验证：54 项测试通过，TypeScript 类型检查通过。这些验证只证明已覆盖的行为，不证明真实房间边界正确。

## 尚未解决

资料室、冷链室、接种室、哺乳室的两轮联合搜索均达到 96 个变体上限，没有生成合格联合候选。当前仅考虑至多两个连接、最多 24 个断口候选，而且组合按输入顺序枚举；因此“没找到”不能推导为“没有可行闭合”。被拒绝的已有 face 即使触发补线搜索，也可能需要删除错误分隔线而不是增加连接。

下一轮应先定位候选召回损失：分别检查原始几何、Jev 区段筛选和组合搜索是否保留正确路径。需要人工确认的参考轮廓才能区分真实漏召回与不存在独立围合。之后再评估拓扑引导的多断口搜索或成组区段选择；不要仅提高搜索上限、放宽接受标准或添加房间 ID 特例。
