# Jev owns semantic decisions

The main CAD interpretation path sends source entities, geometry features, and local context to Jev and uses Jev's typed decisions as the authority for semantic types and relationships. Deterministic layer, block, geometry, and topology logic remains available for diagnostics, request shaping, and evaluation, but it must not replace or overwrite Jev decisions because the goal is to evaluate Jev as the semantic decision layer.
